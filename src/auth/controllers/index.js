const login = (req, res) => {
  return res.redirect("/");
};

const register = (req, res) => {
  return res.redirect("/");
};

const callback = (req, res) => {
  return res.redirect("/");
};

const createOrg = (req, res) => {
  return res.redirect("/");
};

module.exports = {
  login,
  register,
  callback,
  createOrg
};
